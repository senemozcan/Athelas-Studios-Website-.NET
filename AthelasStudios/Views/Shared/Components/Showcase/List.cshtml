@model IEnumerable<Game>

<div class="mt-5 text-center border-bottom">
    <h6 class="display-6">Games</h6>
</div>

<div class="row">
    @foreach (var prd in Model)
    {
        <div class="col-md-3 my-3">
            <div class="card" style="width: 18rem;">
                @* <img class="card-img-top" src="@prd.ImageUrl" alt="Card image cap"> *@
                <div class="card-body">
                    <h5 class="card-title">@prd.GameName</h5>
                    <p class="card-text">@prd.GamePrice</p>
                    <a asp-action="Update" asp-controller="Game" asp-route-id="@prd.GameId"
                        class="btn btn-primary">Edit</a>
                </div>
                <div class="card-footer">
                    <div class="d-flex justify-content-between">

                        <div class="form-check form-switch">
                            <input type="hidden" name="gameId" value="@prd.GameId" />
                            <input class="form-check-input" type="checkbox" id="flexSwitchCheckDefault"
                                checked="@prd.ShowCase" disabled>
                            <label class="form-check-label" for="flexSwitchCheckDefault">Showcase</label>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
</div>